<script type="text/javascript">
      google.charts.load('current', {'packages':['bar']});
      google.charts.setOnLoadCallback(drawChart);

      function drawChart() {
        
        var data = google.visualization.arrayToDataTable([
          ['Mês', 'Vendas'],
          <% @valor_vendas.each do |k,v| %>
            [ '<%= converte_mes(k) %>', <%= v %>],
          <% end %>
        ]);


        var options = {
          chart: {
            title: 'Valor em vendas(R$:)'
            
          },
          bars: 'vertical',
          vAxis: {format: 'decimal'},
          height: 300,
          width: 400,
          legend: { position: 'none' },
          colors: ['#d95f02']
        };

        var chart = new google.charts.Bar(document.getElementById('columnchart_material_valor_vendas'));

        chart.draw(data, google.charts.Bar.convertOptions(options));
  
  // pequisa e agrupa por mes e soma valor das vendas
  //Venda.where("extract( month from created_at) = ?", Time.new.month).group('extract( month from created_at)').sum('valor_total')
      }
    </script>

  
  