<div class="login-container">
	<div class="login-panel panel-cadastrar">
		<h2>Inscrever-se</h2>

		<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
		  <%= f.error_notification %>

		  <div class="form-inputs">
		    <%= f.input :email, required: true, autofocus: true %>
		    <%= f.input :nome, required: true %>
		    <%= f.input :password, required: true, hint: ("#{@minimum_password_length} Mínimo de caracteres" if @minimum_password_length) %>
		    <%= f.input :password_confirmation, required: true %>
		  </div>

		  <div class="form-actions">
		    <%= f.button :submit, "Inscrever-se", class: 'btn btn-primary' %>
		  </div>
		<% end %>

		<%= render "devise/shared/links" %>
</div>