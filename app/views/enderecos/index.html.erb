<p id="notice"><%= notice %></p>

<h1>Endereços</h1>

<table>
  <thead>
    <tr>
      <th>End</th>
      <th>Numero</th>
      <th>Complemento</th>
      <th>Cliente</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @enderecos.each do |endereco| %>
      <tr>
        <td><%= endereco.end %></td>
        <td><%= endereco.numero %></td>
        <td><%= endereco.complemento %></td>
        <td><%= endereco.cliente %></td>
        <td><%= link_to 'Mostar', endereco %></td>
        <td><%= link_to 'Editar', edit_endereco_path(endereco) %></td>
        <td><%= link_to 'Deletar', endereco, method: :delete, data: { confirm: 'Deseja Excluir?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Endereco', new_endereco_path %>
